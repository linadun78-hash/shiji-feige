# 版本记录

## 0.4.0 本地自动启动预览

- Windows 用户首次安装 Python 依赖并注册 Native Messaging 后，可由扩展按需唤起后台服务。
- 新增后台启动、状态查询、停止及注销脚本；重复启动复用已有服务，端口占用不终止其他进程。
- 扩展新增“启动并连接”操作以及未注册、端口占用和启动失败状态。
- 新增 16/32/48/128 PNG 飞鸽图标；固定源码扩展 ID。旧版升级请先导出收集箱，数据不会自动迁移至新 ID。
- 保留原 HTTP/MCP 地址、批次读取命令、源码公开及保留权利边界。

仍需 Python 3.10+、手动加载扩展及 Agent 配置；未提供免 Python 安装包、托盘、开机自启、商店版本或服务端持久化。

## 0.3.0 源码预览

- 增加跨网页收集箱，每条材料绑定独立来源、选取目的、要求和回复语言。
- 支持任务预设、参考标记、勾选排序、整体要求与批次 Markdown 导出。
- 增加 2.0 任务包，兼容旧单条结构；本地 HTTP 与 MCP 共用快照存储。
- 增加准确批次 ID 的读取提示和 MCP 读取回执。
- 支持共享收集箱的多标签更新与编辑冲突提示。
- 悬浮入口可拖动；支持划选、区域与正文三种采集。

本版本仍需手动加载扩展、启动 Python 服务和配置 Agent。未提供 Windows 整合包、配对授权、长期存储、云端服务、PDF/OCR 或扩展商店安装。
