# 0.3.0 源码发布副本验证

日期：2026-09-11。验证对象为按文件白名单生成的独立源码目录，不包含父工作区或其 Git 历史。

环境：Windows、Node 24.12.0、Python 3.12.7、Chromium 131；使用已有 Python 和 Playwright 运行时。不是无 Python 的 Windows 安装包验收，也不是全新操作系统验收。

| 检查 | 结果 |
| --- | --- |
| npm ci --ignore-scripts --offline --no-audit --no-fund | 通过；从缓存恢复锁定的 Node 依赖 |
| npm run build | 通过；在发布目录重新生成扩展资源 |
| npm test | 11/11 通过 |
| python -m pytest -c pytest.ini tests -q | 14/14 通过；2 条 websockets 依赖弃用警告 |
| node tests/browser-send.cjs | 通过；两页收集、要求/语言对应、并发编辑与预设冲突、排序选择、导出、HTTP/MCP/回执、幂等重试、断线保留与三个视口 |
| browser-send 的完整浏览器重启验证 | 通过；收集箱与发送预设保留 |
| node tests/browser-isolation.cjs | 通过；未经修改的正式封闭根扩展可采集及编辑，宿主普通 DOM 不能直接读取其他来源材料 |

发送测试使用临时服务端口 18766、临时浏览器配置和合成内容，不访问用户正在运行的 8766 服务。公开演示图裁剪自上述测试截图，只有合成材料。

按白名单复制了 45 个运行、构建和测试文件，另编写公开使用说明。未包含内部交接、真实任务、浏览器配置、运行日志、缓存、环境文件或整个依赖目录。对文本文件检查了个人机器路径、已知用户快照 ID、常见凭据格式和私钥标记，未发现匹配。这是定向发布检查，不是全面安全审计。

尚未通过的交付条件：原生 Codex 工具加载、无开发环境的 Windows 整合包、本地配对鉴权、持久化快照、Edge/Chrome 商店安装和 PDF/OCR。源码预览保留这些已知限制，不宣称它们已完成。
