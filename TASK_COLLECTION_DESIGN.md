# 拾集 · 飞鸽：材料与任务要求绑定方案

状态：0.3.0 原型已实现并完成真实扩展与官方 MCP 客户端联调；用户操作体验、原生 Codex 工具加载和回答效果待验收。依据 2026-09-10 用户需求实现。

## 目标与现状

减少用户在多个网页与 Agent 对话之间反复复制、补来源、写要求、整理顺序的操作。核心单位从一段长文本变为“材料 + 来源 + 用户任务要求”。

原 0.2.0 只有一份页面草稿，追加会拼接文本，禁止跨来源追加。现 0.3.0 保留页面草稿，并增加扩展共享收集箱与 2.0 任务包；每条材料独立绑定来源、目的、要求与语言。PDF/OCR 仍未实现，本功能不解决不可读取的内容。

## 推荐交互

1. 保留悬浮入口和三种采集方式。拾取后显示原文预览及“这条材料要做什么”的输入框，支持自由输入或选择常用要求后编辑。
2. 保存为收集项，自动绑定标题、网址、采集时间和稳定 ID。未填目的或要求的条目保留为待补全任务；只有用户明确勾选时才标为“仅作参考”，发送前检查。
3. 可继续在同页或其他已启用的标签页采集。收集箱由扩展统一管理，不能再只放在网页脚本内存中；保存草稿与向 Agent 提交是两个独立动作。
4. 收集箱用紧凑列表显示编号、来源、内容摘要和要求。展开单条编辑；支持勾选、移除和调整顺序。
5. 可选填写“整体要求”，例如比较材料 1、2 的观点，或把结论整理为报告。
6. 确认后一次提交勾选条目及整体要求，生成一个批次 ID 和一条 Agent 调用提示。Agent 按编号读取材料和要求，不把多段原文混成一个来源。

填写要求不强制打断连续采集：可以当场填写，也可先入箱再补充。第一版提供自由输入与少量常用要求，不建设复杂指令市场。

## 示例

| 编号 | 材料 | 该条要求 |
| --- | --- | --- |
| 1 | 招聘页 A 的岗位职责 | 提炼必须具备的能力，引用对应原文 |
| 2 | 公司 B 的产品介绍 | 判断团队主要面向哪些用户 |
| 3 | 文章 C 的一段观点 | 找出论据不足的地方，说明缺少什么证据 |

整体要求：按 1、2、3 分别回答，最后整理出还需要核实的信息，并保留来源链接。

当前为逐条选择模板并编辑要求，不默认沿用上一条目的和要求；批量应用模板尚未实现。默认回复语言可在批次设置，逐条明确选择的语言优先。跨材料比较通过整体要求引用当前选中条目的编号；更改顺序/勾选后需要用户核对手写引用，后续再考虑独立多材料分组。

## 实现边界

- 扩展只组织和传递，不在采集期间调用大模型分析。
- 用户要求与网页原文使用不同字段；网页中的指令仍是参考材料，不能自动变成用户任务。
- 第一版统一发送指一次提交任务包，再由用户在 Agent 中发送一条调用提示。MCP 本身不保证主动触发任意聊天窗口。
- “MCP 已读取”不是“任务已完成”；结果自动回填需要另行实现。
- 提交后冻结快照，后续编辑不改变已发批次；再次发送创建新版本，失败重试避免重复。
- 扩展后台统一处理多页签编辑，避免覆盖；明确刷新/浏览器重启后的草稿保留策略及删除入口。
- 大批次显示条目数与总字符数，超限提示拆分，不静默丢材料、来源或要求。
- 服务不可用时仍可导出相同结构的任务 Markdown，保留所有对应关系。

## 数据与协议

任务包包含批次 ID、版本、整体要求和有序条目。每条包含稳定 ID、展示编号、来源、最终编辑内容、用户要求和任务/参考标记。排序只改展示顺序，不改变绑定关系。

未来 PDF 的页码、区域和识别方式属于来源元数据，可接入同一结构。旧 get_selected_context 单条读取保持兼容，批次读取增加明确类型和结构化返回，不把批次伪装成一个网页。

本方案不增加云端账户或第三方模型服务。跨设备同步、结果回填、自动执行终端命令不属于本轮范围。

实现入口：`extension/src/collection-core.js`、`background.js`、`content.js`、`panel.html`；后端为 `server/context_schema.py`、`context_store.py`、`app.py`、`mcp_server.py`。收集箱保存到扩展本地存储，未入箱编辑只在当前页面；服务端提交仍是最近 20 份内存快照。旧 1.0 单条协议和工具名称兼容。

## 实施与验收顺序

1. 定义逐条材料/要求及批次结构，验证多来源序列化不丢绑定关系。
2. 扩展后台实现本地收集箱，支持跨页采集、刷新保留、编辑、勾选、排序和删除。
3. 按现有拾集视觉加入单条要求编辑和列表，让用户验收连续采集手感。
4. 已接入统一提交、批次读取和任务 Markdown；真实 Codex 工具加载仍需另行验收。
5. 用相同任务对比手动复制和飞鸽：从 3 个网页选择 5 条材料，绑定不同要求，统一分析。记录耗时、切换次数、来源遗漏和要求错配，不提前宣传节省比例。

测试覆盖：换页来源不混、排序要求不串、删除条目不发送、参考条目不擅自执行、提交与预览一致、重试不重复、刷新不丢收集箱、多标签新增不覆盖。

## 发布顺序

先发布 GitHub 本地源码预览，随后提供经过独立验收的 Windows 整合包，再上架 Edge 和 Chrome。继续验证“多条材料 + 对应要求 + 一次交付”的价值及可靠 MCP 接入。PDF/OCR 保留为后续需求，等待具体文档样例确定范围。
